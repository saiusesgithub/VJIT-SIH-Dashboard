import { NextRequest, NextResponse } from "next/server"; import { ADMIN_SESSION_COOKIE, isSameOriginRequest, verifyAdminSessionToken } from "@/lib/admin-auth"; import { createJudge } from "@/lib/repositories/admin-management-repository";
export async function POST(request: NextRequest) { if (!isSameOriginRequest(request) || !(await verifyAdminSessionToken(request.cookies.get(ADMIN_SESSION_COOKIE)?.value))) return new Response("Forbidden", { status: 403 }); const form = await request.formData(); await createJudge(Object.fromEntries([...form].map(([k,v])=>[k,String(v)]))); return NextResponse.redirect(new URL("/admin/manage", request.url),303); }
