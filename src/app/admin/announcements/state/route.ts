import { type NextRequest, NextResponse } from "next/server"; import { ADMIN_SESSION_COOKIE, isSameOriginRequest, verifyAdminSessionToken } from "@/lib/admin-auth"; import { updateAnnouncementState } from "@/lib/repositories/operations-repository";
export async function POST(request: NextRequest) { if (!isSameOriginRequest(request) || !(await verifyAdminSessionToken(request.cookies.get(ADMIN_SESSION_COOKIE)?.value))) return new Response("Forbidden", { status: 403 }); const form = await request.formData(); await updateAnnouncementState(String(form.get("id") ?? ""), String(form.get("action") ?? "")); return NextResponse.redirect(new URL("/admin/announcements", request.url), 303); }
