import { getAdminManagementData } from "@/lib/repositories/admin-management-repository";
const field = "mt-1 h-10 w-full rounded-lg border border-zinc-300 px-3 text-sm";
export default async function AdminManagePage() { const data = await getAdminManagementData(); return <div className="space-y-6"><div><p className="text-xs font-medium text-blue-700">Event setup</p><h1 className="mt-1 text-2xl font-semibold">Manage event data</h1><p className="mt-1 text-sm text-zinc-500">Add venues, teams, and judges. Reassign a venue judge without deleting the existing assignment.</p></div><div className="grid gap-5 xl:grid-cols-2"><form action="/admin/manage/venues" method="post" className="rounded-xl border bg-white p-5"><h2 className="font-semibold">Add venue</h2><div className="mt-4 grid gap-3 sm:grid-cols-2"><label className="text-xs font-medium">Code<input name="code" required className={field}/></label><label className="text-xs font-medium">Venue name<input name="name" required className={field}/></label><label className="text-xs font-medium">Room<input name="roomNumber" required className={field}/></label><label className="text-xs font-medium">Building<input name="building" className={field}/></label><label className="text-xs font-medium sm:col-span-2">Theme<input name="theme" className={field}/></label></div><button className="mt-4 h-10 rounded-lg bg-zinc-950 px-4 text-sm font-medium text-white">Add venue</button></form><form action="/admin/manage/teams" method="post" className="rounded-xl border bg-white p-5"><h2 className="font-semibold">Add team</h2><div className="mt-4 grid gap-3 sm:grid-cols-2"><label className="text-xs font-medium">Venue<select name="venueId" required className={field}>{data.venues.map(v=><option key={v.id} value={v.id}>{v.name} · {v.roomNumber}</option>)}</select></label><label className="text-xs font-medium">Team code<input name="teamCode" required className={field}/></label><label className="text-xs font-medium sm:col-span-2">Team name<input name="teamName" required className={field}/></label><label className="text-xs font-medium">Problem code<input name="problemCode" required className={field}/></label><label className="text-xs font-medium">Problem title<input name="problemTitle" required className={field}/></label></div><button className="mt-4 h-10 rounded-lg bg-zinc-950 px-4 text-sm font-medium text-white">Add team</button></form></div><div className="grid gap-5 xl:grid-cols-2"><form action="/admin/manage/judges" method="post" className="rounded-xl border bg-white p-5"><h2 className="font-semibold">Add judge</h2><div className="mt-4 grid gap-3 sm:grid-cols-2"><label className="text-xs font-medium">Name<input name="name" required className={field}/></label><label className="text-xs font-medium">Phone<input name="phone" required className={field}/></label><label className="text-xs font-medium">Password<input name="password" type="password" required minLength={6} className={field}/></label><label className="text-xs font-medium">Venue<select name="venueId" required className={field}>{data.venues.map(v=><option key={v.id} value={v.id}>{v.name} · {v.roomNumber}</option>)}</select></label><label className="text-xs font-medium">Role<select name="role" className={field}><option value="EXTERNAL">External</option><option value="INTERNAL">Internal</option></select></label></div><button className="mt-4 h-10 rounded-lg bg-zinc-950 px-4 text-sm font-medium text-white">Add judge</button></form><form action="/admin/manage/assignments" method="post" className="rounded-xl border bg-white p-5"><h2 className="font-semibold">Assign or change venue judge</h2><div className="mt-4 grid gap-3 sm:grid-cols-2"><label className="text-xs font-medium">Venue<select name="venueId" required className={field}>{data.venues.map(v=><option key={v.id} value={v.id}>{v.name} · {v.roomNumber}</option>)}</select></label><label className="text-xs font-medium">Judge<select name="judgeId" required className={field}>{data.judges.map(j=><option key={j.id} value={j.id}>{j.name}</option>)}</select></label><label className="text-xs font-medium">Role<select name="role" className={field}><option value="EXTERNAL">External</option><option value="INTERNAL">Internal</option></select></label><label className="flex items-end gap-2 pb-2 text-sm"><input name="primary" type="checkbox"/> Make primary judge</label></div><button className="mt-4 h-10 rounded-lg bg-zinc-950 px-4 text-sm font-medium text-white">Save assignment</button></form></div></div>; }
