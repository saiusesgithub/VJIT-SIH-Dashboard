import { getSubmissionOverview, standardSubmissionTypes } from "@/lib/repositories/operations-repository";
const short = { GITHUB: "GitHub", PRESENTATION: "PPT", DEMO: "Demo", PROTOTYPE: "Prototype", VIDEO: "Video", DOCUMENTATION: "Docs" } as const;
export default async function AdminSubmissionsPage() { const teams = await getSubmissionOverview(); return <div className="space-y-5"><div><p className="text-xs font-medium text-blue-700">Project resources</p><h1 className="mt-1 text-2xl font-semibold">Team submissions</h1><p className="mt-1 text-sm text-zinc-500">Missing links are immediately visible across all venues.</p></div><div className="overflow-x-auto rounded-xl border border-zinc-200 bg-white"><table className="w-full min-w-[760px] text-left text-xs"><thead className="border-b border-zinc-200 bg-zinc-50"><tr><th className="px-4 py-3">Team</th><th className="px-4 py-3">Venue</th>{standardSubmissionTypes.map((type) => <th key={type} className="px-3 py-3 text-center">{short[type]}</th>)}</tr></thead><tbody className="divide-y divide-zinc-100">{teams.map((team) => <tr key={team.id}><td className="px-4 py-3"><p className="font-semibold">{team.code}</p><p className="text-zinc-500">{team.name}</p></td><td className="px-4 py-3 text-zinc-600">{team.venue}</td>{standardSubmissionTypes.map((type) => <td key={type} className="px-3 py-3 text-center font-semibold text-zinc-600">{team.submitted.has(type) ? "✓" : "—"}</td>)}</tr>)}</tbody></table></div></div>; }
