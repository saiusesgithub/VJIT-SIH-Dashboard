import { type NextRequest, NextResponse } from "next/server"; import { isSameOriginRequest } from "@/lib/admin-auth"; import { ADMIN_SESSION_COOKIE, verifyAdminSessionToken } from "@/lib/admin-auth"; import { setFeedbackVisibility } from "@/lib/repositories/operations-repository";
export async function POST(request: NextRequest) { if (!isSameOriginRequest(request) || !(await verifyAdminSessionToken(request.cookies.get(ADMIN_SESSION_COOKIE)?.value))) return new Response("Forbidden", { status: 403 }); const form = await request.formData(); await setFeedbackVisibility(String(form.get("roundId") ?? ""), form.get("visible") === "true"); return NextResponse.redirect(new URL("/admin/feedback", request.url), 303); }
