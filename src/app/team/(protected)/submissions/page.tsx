import { getTeamPortalData, submissionLabels, submissionTypes } from "@/lib/repositories/team-repository";
import { requireTeamSession } from "@/lib/require-team-session";
export default async function TeamSubmissionsPage({ searchParams }: { searchParams: Promise<{ status?: string }> }) { const data = await getTeamPortalData(await requireTeamSession()); if (!data) return null; const params = await searchParams; const byType = new Map(data.submissions.map((s) => [s.type, s])); return <div className="space-y-4"><div><p className="text-xs font-semibold text-blue-700">Project resources</p><h1 className="mt-1 text-2xl font-semibold">Submissions</h1><p className="mt-1 text-sm text-zinc-500">Save links only. You can update them throughout the event.</p></div>{params.status === "saved" ? <p className="rounded-lg border border-emerald-200 bg-emerald-50 p-3 text-sm text-emerald-700">Submissions saved.</p> : params.status === "invalid" ? <p className="rounded-lg border border-red-200 bg-red-50 p-3 text-sm text-red-700">Enter valid http:// or https:// links.</p> : null}<form action="/team/submissions/save" method="post" className="space-y-4 rounded-xl border border-zinc-200 bg-white p-4 sm:p-5">{submissionTypes.map((type) => <label key={type} className="block text-xs font-medium text-zinc-700">{submissionLabels[type]}<input name={type} type="url" inputMode="url" defaultValue={byType.get(type)?.url ?? ""} placeholder="https://" maxLength={2048} className="mt-2 h-11 w-full rounded-lg border border-zinc-300 px-3 text-sm outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-100" /></label>)}<button className="h-11 w-full rounded-lg bg-zinc-950 text-sm font-semibold text-white hover:bg-zinc-800">Save Submissions</button></form></div>; }
